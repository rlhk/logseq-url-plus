{
  "name": "logseq-url-plus",
  "version": "0.0.1",
  "description": "Retreive metadata or API response a valid URL and format with various templates.",
  "author": "https://github.com/rlhk",
  "main": "./dist/index.html",
  "license": "MIT",
  "logseq": {
    "id": "logseq-url-plus",
    "title": "URL+",
    "main": "dist/index.html",
    "icon": "dist/icon.png",
    "favicon": "dist/favicon.ico"
  },
  "scripts": {
    "dev": "rm -fr ./dist ./out/* && yarn && mkdir ./dist &&  cp resources/* ./dist/ && npx shadow-cljs watch :plugin :test",
    "release": "rm -fr ./dist ./out/* && yarn && mkdir ./dist &&  cp resources/* ./dist/ && npx shadow-cljs release :plugin",
    "test": "npx shadow-cljs watch :test"
  },
  "devDependencies": {
    "shadow-cljs": "2.20.5"
  },
  "dependencies": {
    "link-preview-js": "^3.0.4"
  }
}
